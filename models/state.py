#!/usr/bin/python3
"""Define the state class."""
from models.base_model import BaseModel

class State(BaseModel):
    """Represent the state 
    Attributes:
    name: the state name.
    """
    name = ""
